@model ExposureAPI.Models.Gallery
@await Component.InvokeAsync("Script", new
{
    className = "GalleryShow"   
})

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>@Model.Title</h2>

<ul/>
<li>
    <il>@Html.Partial("_Uploader")</il>
</li>
   @foreach (var image in Model.Images)
   {
       <li>
           <a href=@"/Gallery/Image/Destroy/{image.imageId}"></a>
           <img src="@image.Path" style="max-height: 50px; max-width: 50px;"/>
       </li>
   }
</u>
